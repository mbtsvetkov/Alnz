packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

  # ----------------------------------------------------------------------------------
  # Optional / advanced tiers. Uncomment to install, then set the matching var in
  # dbt_project.yml (use_dbt_expectations / use_elementary) to activate. Models and
  # tests reference these only behind the var, so a minimal install can leave them
  # commented and `dbt deps` pulls only dbt_utils.
  # ----------------------------------------------------------------------------------

  # - package: metaplane/dbt_expectations
  #   version: [">=0.10.0", "<0.11.0"]

  # - package: elementary-data/elementary
  #   version: [">=0.16.0", "<0.17.0"]
