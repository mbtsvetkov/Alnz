"""dbt_yaml_generator — generate per-model dbt schema YAML from a column-inventory source.

Self-contained, drop-in package: zip this folder, unzip into a dbt project (the repo with
dbt_project.yml), configure config.yml, and run `python -m dbt_yaml_generator`.
"""

__version__ = "1.0.0"
